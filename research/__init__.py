"""
研究/诊断脚本包

`tools/` 下的脚本会 `from research.xxx import ...`。之前这里没有 __init__.py，
能跑起来是靠 Python 3 的**隐式命名空间包**行为 —— 能用，但依赖隐式规则。
显式加上这个文件后，包结构不再依赖命名空间包语义，换环境/换 Python 实现更稳。

注意：本目录下的脚本是**可复现诊断工具**，多数可离线运行（不需要网络/凭据），
配合仓库根目录的 research_out/ 实例图即可复现 README 里的否定性结论。
"""
